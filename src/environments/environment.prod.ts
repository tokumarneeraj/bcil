export const environment = {
  production: true,
  baseUrl:'http://localhost:20039',// 'http://43.224.139.121', // Change this to the address of your backend API if different from frontend address
  tokenUrl: null, // For IdentityServer/Authorization Server API. You can set to null if same as baseUrl
  loginUrl: '/Login'
};
